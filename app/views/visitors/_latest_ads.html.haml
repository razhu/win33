.panel.panel.panel-default
  .panel-body
    %table.table.table-striped.table-hover
      %thead
        %h4 Inmuebles Ofertados Recientemente
      - @advertises.each do |advertise|
        %tbody
          %tr
            %td
              = friendly_date advertise.created_at
            %td
              = link_to advertise.title.truncate(42), advertise_path(advertise)
            %td
              = advertise.location_id
            %td
              = advertise.price
    = link_to raw('Ver todos <i class="fa fa-long-arrow-right"></i>'), advertises_path
